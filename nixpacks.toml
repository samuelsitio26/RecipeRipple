[phases.setup]
nixPkgs = ["php82", "php82Packages.composer", "gcc", "pkg-config"]

[phases.install]
cmds = [
	"install-php-extensions intl zip",
	"composer install --no-dev --optimize-autoloader"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
