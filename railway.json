{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "herokuish"
    },
    "deploy": {
        "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT",
        "healthcheckPath": "/"
    }
}
